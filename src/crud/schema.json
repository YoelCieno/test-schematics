{
  "$schema": "http://json-schema.org/draft-06/schema",
  "id": "CrudSchematics",
  "title": "CRUD Options Schema",
  "description": "First crud",
  "properties": {
    "name": {
      "type": "string",
      "description": "The first CRUD",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What verb do we want to use?"
    },
    "transform": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "name"
  ]
}